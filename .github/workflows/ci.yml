name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  verify:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Type-check
        run: npm run typecheck

      - name: Run tests
        run: npm test

      - name: Cache whisper.cpp build
        uses: actions/cache@v4
        with:
          path: resources/whisper
          key: whisper-${{ runner.os }}-${{ runner.arch }}-v1.7.4

      - name: Build whisper.cpp (if not cached)
        run: npm run whisper:fetch

      - name: Build
        run: npm run build
